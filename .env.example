# Copy to .env and fill in. Never commit .env to version control.

# Required: Discord Incoming Webhook URL (channel → Integrations → Webhooks)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Required: school classes. One per class: label|url, comma-separated.
# Label = how it appears in the digest. Update when classes or school year change.
SCHOOL_CLASSES=ClassA|https://example.com/school/a,ClassB|https://example.com/school/b

# Optional: iCloud/other calendar ICS URLs, comma-separated
# Calendar → Share → Public Calendar / subscription link
# ICS_URLS=https://calendar.icloud.com/...
